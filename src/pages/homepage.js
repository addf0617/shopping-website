import React from "react";
import HeroComponent from "../components/hero-component";

const Homepage = () => {
  return (
    <div>
      <HeroComponent />
      another
    </div>
  );
};

export default Homepage;
